import React from "react";
import { LoadingContainer } from "./styled";

const Loading = () => {
  return (
    <LoadingContainer>
      <div />
    </LoadingContainer>
  );
};

export default Loading;
